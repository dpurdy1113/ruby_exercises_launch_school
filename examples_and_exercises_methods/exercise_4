def scream(words)
  words = words + "!!!!"
  return
  puts words
end

scream("Yippeee")

the code will not print anything to the screen because of the return in the middle of the method stops it from executing the puts words line. 
